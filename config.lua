-- Configuration settings for the Colony_Manager app

-- This file will include configurable parameters such as refresh rates and display settings
-- For now, it's a placeholder

-- Example configuration
local config = {
    refreshRate = 5, -- seconds
    displaySettings = {
        textColor = "white",
        backgroundColor = "black",
        -- other settings will be placed here later
    }
}

return config
